// NOTE: 此测试需要 BehaviorTree.CPP 链接，暂作占位符。
// 第一期迭代中补充真实测试。
#include <gtest/gtest.h>

namespace sim_bt {

TEST(AsyncActionBase, PlaceholderTest) {
  // TODO: 添加基于 mock AsyncActionContext 的 AsyncActionBase 单元测试
  SUCCEED();
}

}  // namespace sim_bt
